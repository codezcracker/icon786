# Optional — only for split deploy (e.g. Vercel frontend + separate API).
# Leave unset for Render single-server (API uses same origin /api).
# VITE_API_URL=https://your-api.example.com

# Optional analytics (production builds only)
# VITE_PLAUSIBLE_DOMAIN=icon786.com
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
