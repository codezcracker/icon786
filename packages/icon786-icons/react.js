/**
 * @icon786/icons/react — use with Icon786 API or self-hosted server.
 * Set ICON786_API_URL or pass apiBase to Icon (future).
 *
 * For now, point your app at your Icon786 server:
 *   fetch(`${API}/api/icons/svg/${prefix}/${name}`)
 */
module.exports = {};

// React component ships with the Icon786 web app (frontend/src/components/Icon.jsx).
// Publish @icon786/react as a separate package when ready.
